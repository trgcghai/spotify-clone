export const metadata = {
    title: "Spotify Clone | Playlist",
    description: "Spotify Clone | Playlist",
}

export default function layout({ children }) {
    return (
        <>{children}</>
    )
}