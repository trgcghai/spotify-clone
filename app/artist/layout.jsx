export const metadata = {
    title: "Spotify Clone | Artists",
    description: "Spotify Clone | Artists",
}

export default function layout({ children }) {
    return (
        <>{children}</>
    )
}