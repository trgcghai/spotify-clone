export const metadata = {
    title: "Spotify Clone | Albums",
    description: "Spotify Clone | Albums",
}

export default function layout({ children }) {
    return (
        <>{children}</>
    )
}