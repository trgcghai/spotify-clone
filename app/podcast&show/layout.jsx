export const metadata = {
    title: "Spotify Clone | Podcast & Show",
    description: "Spotify Clone | Podcast & Show",
}

export default function layout({ children }) {
    return (
        <>{children}</>
    )
}